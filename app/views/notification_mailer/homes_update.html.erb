<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Here are all the listings posted this week!</h1>
    <% @homes.each do |home| %>
      <% if Time.now - home.created_at <= 7.days %>
        <li class="box">
          <%= image_tag(home.image_url(:medium), class:'mainimage') %>
          <div class="textline">
            <p class="firstleftline">$<%= home.price %></p>
            <p><%= home.city %>, <%= home.state %>  <%= home.zip %></p>
          </div>
          <div class="textline">
            <p class="secondleftline">
                  <%= home.square_footage %> SqFeet
            </p>
            <p><%= home.bedrooms %> Bedrooms, <%= home.bathrooms %> Bathrooms</p>
          </div>
          <div>
            <%= link_to home, class: 'btn btn-primary' do %>
              See More
            <% end %>
          </div>
        </li>
      <% end %>
    <% end %>
    <p>
      You can see even more on each of these listings here: <%= link_to "Home Listings", homes_url %>
    </p>
  </body>
</html>
